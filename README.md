<div align="center">
  <img src="https://gitee.com/Elvin-Apocalys/pic-bed/raw/master/1212.webp" alt="Syuan Plugin" width="200">
  
  # Syuan Plugin
  
  **自用工具综合插件 | Yunzai-Bot 生态**
  
  [![GitHub release](https://img.shields.io/badge/version-v1.1.3-blue.svg)](https://github.com/lingyatou/syuan-plugin/releases)
  [![License](https://img.shields.io/badge/license-ISC-green.svg)](LICENSE)
  [![Node.js](https://img.shields.io/badge/node-%3E%3D18.0.0-brightgreen.svg)](https://nodejs.org/)
  [![pnpm](https://img.shields.io/badge/pnpm-10.7.1-orange.svg)](https://pnpm.io/)
  
  提供运维管理、娱乐互动、游戏辅助等一体化能力的 Yunzai/NapCat 插件
  
</div>

---

## 📑 目录

- [✨ 特性](#-特性)
- [🎯 功能概览](#-功能概览)
  - [更新管理](#更新管理)
  - [服务器运维](#服务器运维-ssh)
  - [游戏辅助](#游戏辅助-grasscutter-opencommand)
  - [群组管理](#群组管理)
  - [自动化功能](#自动化功能)
  - [娱乐互动](#娱乐互动)
  - [实用工具](#实用工具)
- [📦 安装](#-安装)
- [⚙️ 配置](#️-配置)
- [📖 使用指南](#-使用指南)
- [🗂️ 目录结构](#️-目录结构)
- [🛠️ 开发指引](#️-开发指引)
- [❓ 常见问题](#-常见问题)
- [🙏 鸣谢](#-鸣谢)
- [📄 许可证](#-许可证)

---

## ✨ 特性

- 🚀 **模块化设计** - 15+ 功能模块，按需启用
- 🔐 **权限管理** - 完善的主人/管理员权限控制
- 🎨 **丰富交互** - 支持文字、图片、转发消息等多种形式
- 🔄 **自动更新** - 内置更新管理，支持常规/强制更新
- ⏰ **定时任务** - 支持自动点赞、定时推送等功能
- 🌐 **远程管理** - SSH 服务器运维，OpenCommand 游戏指令
- 🎮 **游戏集成** - 谁是卧底等互动小游戏
- 🤖 **AI 对话** - 集成 AI 对话能力（需配置）

---

## 🎯 功能概览

### 更新管理

| 指令 | 说明 | 权限 |
|------|------|------|
| `#syuan更新` | 拉取插件最新代码并安装依赖 | 主人 |
| `#syuan强制更新` | 丢弃本地修改，强制更新到最新版本 | 主人 |

### 服务器运维 (SSH)

| 指令 | 说明 | 权限 |
|------|------|------|
| `#memenew` | SSH 连接远程服务器，批量更新表情包仓库并重启服务 | 主人 |

> 需要配置 `data/Syuan-plugin/sshInfo.json`

### 游戏辅助 (Grasscutter OpenCommand)

| 指令 | 说明 | 权限 |
|------|------|------|
| `#oc设置地址 <addr>` | 设置 OpenCommand 服务器地址 | 主人 |
| `#oc设置token <token>` | 手动设置访问 Token | 主人 |
| `#oc查询` | 检查服务连通性 | 主人 |
| `#oc验证码 <uid>` | 向指定 UID 发送验证码 | 主人 |
| `#oc验证 <code>` | 提交验证码完成绑定 | 主人 |
| `#oc执行 <command>` | 远程执行游戏指令 | 主人 |

> 适用于 Grasscutter 私服管理

### 群组管理

| 指令 | 说明 | 权限 |
|------|------|------|
| `#设置群欢迎词` | 为当前群设置欢迎词（支持指定群号） | 管理员 |
| `#群欢迎词` | 查看所有已设置的群欢迎词 | 管理员 |
| `#插件库` | 返回插件仓库地址 | 所有人 |
| `#syuan帮助` | 显示完整帮助菜单 | 所有人 |

### 自动化功能

#### 新人入群欢迎
- 自动发送自定义欢迎词
- 支持文字 + 图片组合
- 可按群配置不同欢迎内容

#### 退群通知
- 自动提示成员退群（含昵称/QQ）
- 需要 NapCat HTTP 接口支持

#### 自动点赞
- **指令**: `#赞我` - 立即为触发者点赞
- **定时**: 每日 10:00 自动为配置列表中的用户点赞
- **推送**: 支持私聊推送点赞通知

配置文件：`data/thumbsUpMe.json`
```json
{
  "123456789": { 
    "push": true,        // 是否推送通知
    "hitokoto": false,   // 是否附加一言
    "group": 987654321   // 关联群号
  }
}
```

### 娱乐互动

#### 戳一戳回复
- 戳指定账号时随机发送图片
- 图片资源目录：`resources/Syuan_plugin/`
- 支持 jpg/png/gif 格式

#### 关键词回复
- 自动识别特定关键词并回复
- 支持文字和图片回复
- 配置文件：`apps/keyword.js`

示例关键词：
- `6` → 触发特定回复
- 包含 "哈气" → 发送图片
- 匹配 "小土豆|土豆雷|i柯TV" → 对应回复

#### 谁是卧底游戏

| 指令 | 说明 |
|------|------|
| `#卧底开始` | 开始新游戏 |
| `#加入卧底` | 加入游戏 |
| `#发词` | 发送词语给玩家 |
| `#投票` | 投票环节 |
| `#结束卧底` | 结束游戏 |

### 实用工具

#### 汇率查询
- 实时汇率转换
- 支持多种货币

#### AI 对话
- 集成 OpenAI 接口
- 支持上下文对话
- 需配置 API Key

---

## 📦 安装

### 环境要求

- **Node.js**: >= 18.0.0
- **包管理器**: pnpm 10.7.1
- **运行环境**: Yunzai-Bot / NapCat

### 安装步骤

1. **克隆插件到 Yunzai 插件目录**

```bash
cd /path/to/Yunzai-Bot
git clone https://github.com/lingyatou/syuan-plugin ./plugins/syuan-plugin
```

2. **安装依赖**

```bash
pnpm install
```

3. **重启 Yunzai**

```bash
pnpm start
```

4. **验证安装**

插件加载成功后会在控制台输出：

```
✨🌸━━━━━━━━ SYUAN-PLUGIN ━━━━━━━━🌸✨
💖 插件名称：syuan-plugin
📦 插件版本：v1.1.3
👩‍💻 作者：源Syuan
📜 描述：自用工具综合插件
✨🌸━━━━━━━━━━━━━━━━━━━━━━━━━━━━━🌸✨
```

---

## ⚙️ 配置

### 1. 全局配置

**文件位置**: `config/cfg.yaml`

```yaml
# 允许列表：仅这些群生效（为空则全部允许）
#allowlist: [123456789, 987654321]

# 禁止列表：这些群不会生效
denylist: [111111111, 222222222]
```

### 2. 数据配置

插件会在 Yunzai 根目录创建数据目录：`data/Syuan-plugin/`

#### SSH 连接配置

**文件**: `data/Syuan-plugin/sshInfo.json`

```json
{
  "host": "192.168.1.100",
  "port": 22,
  "username": "root",
  "password": "your_password",
  "description": "表情包服务器"
}
```

#### OpenCommand 配置

**文件**: `data/Syuan-plugin/opencommand.json`

```json
{
  "host": "http://127.0.0.1:8080",
  "token": "your_access_token"
}
```

> Token 可通过 `#oc验证码` + `#oc验证` 流程自动获取

#### 群欢迎词配置

**文件**: `data/Syuan-plugin/welcome.json`

```json
{
  "123456789": "欢迎新人！请阅读群公告～"
}
```

> 通过 `#设置群欢迎词` 指令自动生成

#### 自动点赞配置

**文件**: `data/thumbsUpMe.json`

```json
{
  "123456789": {
    "push": true,
    "hitokoto": false,
    "group": 987654321
  }
}
```

### 3. 资源文件

#### 帮助菜单资源
- 位置：`resources/help/`
- 包含帮助菜单的 YAML 配置和图片

#### 关键词回复图片
- 位置：`resources/img/`
- 文件：`fbhq.jpg`、`xtd.jpg` 等

#### 戳一戳图片库
- 位置：`resources/Syuan_plugin/`
- 支持格式：jpg、png、gif

---

## 📖 使用指南

### SSH 远程服务器管理

**使用场景**: 批量更新表情包仓库并重启服务

**步骤**:

1. 配置 SSH 连接信息（见上文配置章节）
2. 在群内发送 `#memenew`
3. 插件将自动执行：
   - 连接 SSH 服务器
   - 批量 `git fetch` + `git reset --hard` 所有表情包仓库
   - 重启 `meme_generator.service` 服务

**权限**: 仅主人可用

### Grasscutter OpenCommand

**使用场景**: 远程管理 Grasscutter 私服

**快速开始**:

```bash
# 1. 设置服务器地址
#oc设置地址 http://127.0.0.1:8080

# 2. 获取 Token（二选一）
## 方式A：验证码流程
#oc验证码 10001
#oc验证 123456

## 方式B：手动设置
#oc设置token your_token_here

# 3. 测试连接
#oc查询

# 4. 执行指令
#oc执行 give 10001 223 x10
```

### 群欢迎词

**设置当前群欢迎词**:
```
#设置群欢迎词 欢迎新人加入！请先阅读群公告哦~
```

**设置指定群欢迎词**:
```
#设置群欢迎词 123456789 欢迎来到XX群！
```

**查看所有欢迎词**:
```
#群欢迎词
```

### 点赞功能

**立即点赞**:
```
#赞我
```

**配置自动点赞**:
编辑 `data/thumbsUpMe.json`，添加目标 QQ 号和配置

**定时规则**: 每日 10:00 自动执行（非会员默认 10 次）

---

## 🗂️ 目录结构

```
syuan-plugin/
├── apps/                    # 功能模块目录
│   ├── ai.js               # AI 对话
│   ├── botHelp.js          # 帮助菜单
│   ├── good.js             # 点赞功能
│   ├── gs.js               # Grasscutter OpenCommand
│   ├── huilv.js            # 汇率查询
│   ├── keyword.js          # 关键词回复
│   ├── newcomer.js         # 新人入群
│   ├── other.js            # 其他功能
│   ├── pardofelis_poke.js  # 戳一戳（帕朵菲莉丝）
│   ├── poke.js             # 戳一戳处理
│   ├── run.js              # 运行相关
│   ├── ssh.js              # SSH 服务器管理
│   ├── test.js             # 测试功能
│   ├── underCover.js       # 谁是卧底游戏
│   └── update.js           # 插件更新
├── config/                  # 配置文件目录
│   └── cfg.yaml            # 全局配置
├── data/                    # 数据文件目录（Yunzai根目录）
│   └── Syuan-plugin/
│       ├── sshInfo.json
│       ├── opencommand.json
│       └── welcome.json
├── resources/               # 资源文件目录
│   ├── help/               # 帮助菜单资源
│   ├── img/                # 关键词回复图片
│   └── Syuan_plugin/       # 戳一戳图片库
├── tools/                   # 工具函数目录
│   ├── index.js            # 工具导出
│   ├── admin.js            # 权限管理
│   ├── cfgdata.js          # 配置读写
│   ├── sshData.js          # SSH 数据管理
│   └── versionInfo.js      # 版本信息
├── index.js                 # 插件入口
├── guoba.support.js         # 锅巴插件支持
├── package.json             # 依赖配置
└── README.md                # 项目文档
```

---

## 🛠️ 开发指引

### 添加新功能模块

1. **创建模块文件**

在 `apps/` 目录创建新文件，例如 `myFeature.js`:

```javascript
import plugin from '../../../lib/plugins/plugin.js'

export class MyFeature extends plugin {
  constructor() {
    super({
      name: '我的功能',
      dsc: '功能描述',
      event: 'message',
      priority: 5000,
      rule: [
        {
          reg: '^#测试指令$',
          fnc: 'handleTest'
        }
      ]
    })
  }

  async handleTest(e) {
    await e.reply('测试成功！')
    return true
  }
}
```

2. **导出模块**

在 `apps/index.js` 中，模块会自动加载（无需手动导出）

3. **使用工具函数**

```javascript
import { pluginPath, rootPath, dataPath, versionInfo } from '../tools/index.js'

// 获取路径
const configPath = path.join(pluginPath, 'config', 'myconfig.yaml')

// 读取版本信息
const version = versionInfo.getVersion()
```

### 常用工具

| 工具 | 说明 |
|------|------|
| `pluginPath` | 插件根目录路径 |
| `rootPath` | Yunzai 根目录路径 |
| `dataPath` | 数据目录路径 |
| `cfgdata` | 配置文件读写工具 |
| `sshData` | SSH 配置管理工具 |
| `versionInfo` | 版本信息读取工具 |

### 开发规范

- ✅ 继承 `plugin` 基类
- ✅ 使用 ESM 模块语法
- ✅ 遵循现有代码风格
- ✅ 添加适当的错误处理
- ✅ 权限控制使用 `isMaster` 等方法
- ✅ 配置文件使用 YAML 或 JSON 格式

---

## ❓ 常见问题

### Q: 指令提示"仅主人可用"

**A**: 检查以下内容：
1. 是否在 Yunzai 配置中设置了主人 QQ
2. 当前群是否在 `config/cfg.yaml` 的 `denylist` 中
3. 权限判定逻辑见 `tools/admin.js`

### Q: `#memenew` 执行失败

**A**: 排查步骤：
1. 检查 `sshInfo.json` 配置是否完整
2. 确认服务器 SSH 连接是否正常
3. 验证服务器是否有 git 和 systemd 权限
4. 查看控制台错误日志

### Q: OpenCommand 连接失败

**A**: 检查清单：
1. Grasscutter 服务器是否启动
2. OpenCommand 插件是否已安装
3. `host` 地址和端口是否正确
4. `token` 是否有效（尝试重新验证）

### Q: 自动点赞不生效

**A**: 确认以下配置：
1. `thumbsUpMe.json` 格式是否正确
2. 定时任务是否启动（检查 `node-schedule`）
3. 机器人账号是否有点赞权限

### Q: 插件更新后出现问题

**A**: 尝试以下方法：
1. 执行 `pnpm install` 重新安装依赖
2. 清除 `node_modules` 后重新安装
3. 使用 `#syuan强制更新` 重置到最新版本

---

## 🙏 鸣谢

感谢以下项目和开发者：

- [Yunzai-Bot](https://github.com/Le-niao/Yunzai-Bot) - 优秀的 QQ 机器人框架
- [miao-plugin](https://github.com/yoimiya-kokomi/miao-plugin) - 插件架构参考
- [NapCatQQ](https://github.com/NapNeko/NapCatQQ) - QQ 协议实现

---

## 📄 许可证

本项目基于 [ISC License](LICENSE) 开源。

---

<div align="center">
  
  **如果觉得这个插件有用，请给个 ⭐ Star 支持一下！**
  
  Made with ❤️ by [源Syuan](https://github.com/lingyatou)
  
</div>
